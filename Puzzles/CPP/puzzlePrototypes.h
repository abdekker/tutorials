#pragma once
#include "stdafx.h"
//#include "puzzle0001.h"

// General puzzle prototypes
// Note: Puzzles which contains classes, templates and other more complex data structures will
// generally have a separate header

// Puzzle 0001 uses a separate class
int RunPuzzle0002(const int a, const int b);
// Puzzle 0003 unsolved
void RunPuzzle0004();
ARRAY RunPuzzle0005(ARRAY input);
void RunPuzzle0006(ARRAY* input, const int cMultiplier);
void RunPuzzle0007(MATRIX* input, const int size);
// Puzzle 0008/0009 (currently) solved in Delphi only
int RunPuzzle0010(int startingBricks);
